# deps
node_modules/
.pnp.*
.yarn/*

# build
dist/
build/
.next/
out/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# test / coverage
coverage/
playwright-report/
test-results/
.nyc_output/

# prisma
apps/api/prisma/*.db

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
apps/web/e2e/.auth/
.env.prod

# Operator deploy notes + secrets for the 10.0.0.20 VM rollout — never commit
deploy.md
secrets.md

# temp KB article payload
.kb-api-article.html
# temp seed scripts
.gen-tickets.py
.port-macros.py
.macros.json
.macros-cats.json

# Redis snapshot files (e.g. from a local redis-server used for testing)
*.rdb

# VM-owned runtime state and backup artifacts — never sync/commit with a release
backups/
**/backups/
uploads/
**/uploads/
*.dump
*.dump.gz
*.tar.gz
